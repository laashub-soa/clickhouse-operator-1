{{- if .Values.metricService }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "clickhouse-operator.name" . }}-metrics
  namespace: {{ .Release.Namespace }}
  labels:
    component: app
    app: {{ template "clickhouse-operator.name" . }}-metrics
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
spec:
  selector:
    app: {{ template "clickhouse-operator.name" . }}
  ports:
  - name: metrics
    port: 9710
    protocol: TCP
{{- end }}
